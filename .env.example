PORT=8080
DATABASE_URL=postgresql://jfa_user:change_me_db_password@db:5432/jfa_secure
API_TOKEN=change_me_super_long_random_token
# 32-byte key in base64/hex/plain-text(32 chars). Generate with:
# node -e "console.log(require('crypto').randomBytes(32).toString('base64'))"
APP_DATA_KEY=replace_with_32_byte_key_base64
ALLOWED_ORIGIN=*
SERVE_STATIC=true
STATIC_DIR=/app/static
APP_PUBLIC_URL=http://localhost:8081
SMTP_HOST=
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=
SMTP_PASS=
SMTP_FROM="Matla JFA <no-reply@matla.local>"
SMTP_REPLY_TO=
MAX_PAYLOAD_BYTES=10485760
SESSION_JWT_SECRET=replace_with_long_random_secret
SESSION_TOKEN_TTL_SECONDS=28800
SESSION_REFRESH_TOKEN_TTL_SECONDS=1209600
SESSION_IDLE_TIMEOUT_SECONDS=43200
ENFORCE_SIGNED_ROLE=true
REQUIRE_2FA_FOR_PRIVILEGED=false
TOTP_WINDOW_STEPS=1
LOGIN_MAX_FAILED_ATTEMPTS=8
LOGIN_LOCKOUT_MINUTES=15
SMTP_QUEUE_RETRY_LIMIT=5
SMTP_QUEUE_BATCH_SIZE=20
FA_ASSIGN_MAX_PENDING_PER_FA=60
LEAD_ASSIGN_SLA_HOURS=24
